name: Profile Activity Chart

# Deprecated: the live portfolio now renders repository activity directly
# from GitHub's public API in src/components/GitHubActivityChart.tsx.
# Kept as a manual workflow for historical compatibility; it no longer
# generates or commits a static chart artifact.

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "Static activity chart generation is deprecated."
          echo "The live portfolio uses GitHubActivityChart.tsx instead."
